<%#
# Refile Form Partial

This partial renders an attachment_field element for uloading files.

## Local variables:

- `f`:
  A Rails form generator, used to help create the appropriate input fields.
- `field`:
  An instance of [Administrate::Field::Refile][1].

%>

<div class="field-unit__label">
  <%= f.label field.attribute %>
</div>
<div class="field-unit__field">
  <%= f.attachment_field field.attribute, direct: field.direct, presigned: field.presigned, multiple: field.multiple %>
  <% if @requested_resource.class.to_s == "Instance" && field.attribute.to_s == "hero_image" %>
    <%= link_to "delete image", "/admin/instances/#{@requested_resource.id}/delete_hero_image", method: :delete %>
  <% elsif @requested_resource.class.to_s == "Instance" && field.attribute.to_s == "homepage_video" %>
    <%= link_to "delete video", "/admin/instances/#{@requested_resource.id}/delete_homepage_video", method: :delete %>
  <% end %>
</div>
